const SliderIni = document.getElementById('SliderIni')

SliderIni.addEventListener('slide.bs.carousel', event => {
  



})




const carousel = new bootstrap.Carousel(SliderIni, {
    interval: 2000,
    touch: false
  })